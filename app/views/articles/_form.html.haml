= form_for @article do |f|
  - if @article.errors.any?
    %h4 Errors:
    %ul
      - @article.errors.full_messages.each do |message|
        %li= message
  %p
    = f.label :title
    = f.text_field :title
  %p
    = f.label :body
    = f.text_field :body
  %p
    = f.submit