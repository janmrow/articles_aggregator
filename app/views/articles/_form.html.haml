= form_for @article do |f|
  - if @article.errors.any?
    %h4 Errors:
    %ul
      - @article.errors.full_messages.each do |message|
        %li= message
  %p
    = f.label :title
    %br
    = f.text_field :title
  %p
    = f.label :body
    %br
    = f.text_area :body
  %p
    = f.submit